# Spec Interpretation: Task 5 - agents/spec/spec-validator.md にエントリー基準プレチェックを追加

## 対象要件
- Requirement: REQ-005 spec-validator のエントリー基準プレチェック
  - **仕様の記述**:
    - GIVEN spec-validator.md に Step 0 が定義されている WHEN Step 0 の内容を確認する THEN proposal.md の存在チェック、必須セクション（Intent, User Stories/Scope, Out of Scope）の存在チェック、ユーザーストーリーの最低1つの存在チェックが含まれている
    - GIVEN Step 0 の全条件が PASS した WHEN 次のステップを確認する THEN Step 1（対象ファイルの読み込み）に進む
    - GIVEN Step 0 で proposal.md が存在しない WHEN 結果を確認する THEN エラーを出力し検証を中止する（ブロッキング）
    - GIVEN Step 0 で必須セクションが不足している WHEN 結果を確認する THEN エラーを出力し検証を中止する（ブロッキング）
    - GIVEN Step 0 で proposal.md に必須セクションの見出しは存在するが内容が空の場合 WHEN エントリー基準を評価する THEN 見出しのみの存在は条件を満たすが、ユーザーストーリーの最低1つ存在チェックで FAIL する
  - **私の解釈**: spec-validator.md の「## ワークフロー」セクション内、「### Step 1: 対象ファイルの読み込み」（行220）の直前に「### Step 0: エントリー基準プレチェック」を挿入する。タスク指示で提供された Markdown テキストをそのまま挿入する。既存の Step 番号（Step 1〜5）は変更しない。
  - **前提（仕様に未記載だが私が仮定すること）**: 挿入位置は行218（`## ワークフロー`）と行220（`### Step 1`）の間の空行（行219）に、Step 0 のブロック全体を挿入する。
  - **仕様でカバーされないエッジケース**: 本タスクは Markdown ファイルへのテキスト追加であり、プログラムコードの実装ではないため、コード上のエッジケースは該当しない。

## 実装判断

| 判断項目 | 選択肢 | 採用 | 根拠 | 却下理由 |
|---|---|---|---|---|
| 挿入内容 | A: タスク指示のテキストをそのまま使用 | A | タスク指示で挿入内容が明確に指定されている | 代替案なし -- タスク指示が一意に指定 |
| 挿入位置 | A: Step 1 の直前 | A | タスク指示とデルタスペックの両方で「Step 1 の前に Step 0 を追加」と明記 | 代替案なし -- 仕様が一意に指定 |
| 既存 Step 番号の扱い | A: 変更しない（Step 0 を新規追加） | A | タスク指示で「既存の Step 番号は変更しない」と明記 | 代替案なし -- 仕様が一意に指定 |

## 必須チェック項目
- [x] 入力の有効値/無効値は何か？ → 本タスクは Markdown テキストの挿入であり、入力検証は該当しない
- [x] エラー時の振る舞いは？ → Step 0 の FAIL 時の振る舞いは挿入するテキスト内に「検証を中止する（ブロッキング）」と記述されている
- [x] 外部依存が失敗した場合は？ → 該当なし（Markdown ファイル編集のみ）
- [x] 非同期処理の場合、競合状態への対処は？ → 該当なし

## ギャップ検出（エスカレーション候補）
- なし

---

## Phase B: 実装完了後の追記

### 変更ファイル一覧

#### 作成したファイル
- なし

#### 修正したファイル
- `agents/spec/spec-validator.md`: 「## ワークフロー」セクション内の「### Step 1」の直前に「### Step 0: エントリー基準プレチェック」を挿入
- `openspec/changes/w-model-left-review/traceability.md`: Forward Traceability の T-5 行に impl パスを追記、Backward Traceability に T-5 行を追加

#### 削除したファイル
- なし

### 実装の振り返り
- 仕様の曖昧性への対処: タスク指示で挿入内容が完全に指定されていたため、曖昧性は発生しなかった
- 却下した代替案: なし -- 挿入内容・位置・既存 Step の扱い全てがタスク指示で一意に指定されていた
- 想定外の発見: なし
